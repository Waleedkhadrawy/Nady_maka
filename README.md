git config --global user.name "Waleedkhadrawy"
git config --global user.email "waleed.khadrawy.pg@fci.s-mu.edu.eg"

git add README.md
git commit -m "first commit"
git branch -M main
git push -u origin main
